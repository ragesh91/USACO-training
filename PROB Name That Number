/*
ID: ragesh91
PROG: namenum
LANG: C++
*/

#include<iostream>
#include<string.h>
#include<fstream>
using namespace std;

int main(){
   
    ifstream in("namenum.in");
    ofstream out("namenum.out");
    
    ifstream dict("dict.txt");
    
    string names[5000];
    
    long long serial,num=0,temp;
    
    int i,j,count=0,len=0,find=0;
    
    for(i=0;i<5000;i++){
        names[i] = "";
        dict >> names[i];
        if(names[i]!="")
            count++;
    }
    
    in >> serial;
    
    temp = serial;
    len = 0;
    while(temp){
        len++;
        temp /= 10;
    }
    
    for(i=0;i<count;i++){
        if(len != names[i].length())
            continue;
        else{
            num = 0;
            for(j=0;j<len;j++){
                switch (names[i][j]){
                    case 'A':
                    case 'B':
                    case 'C':
                        num = (num*10)+2;
                        break;
                    case 'D':
                    case 'E':
                    case 'F':
                        num = (num*10)+3;
                        break;  
                    case 'G':
                    case 'H':
                    case 'I':
                        num = (num*10)+4;
                        break;    
                    case 'J':
                    case 'K':
                    case 'L':
                        num = (num*10)+5;
                        break;
                    case 'M':
                    case 'N':
                    case 'O':
                        num = (num*10)+6;
                        break;
                    case 'P':
                    case 'R':
                    case 'S':
                        num = (num*10)+7;
                        break;
                    case 'T':
                    case 'U':
                    case 'V':
                        num = (num*10)+8;
                        break;
                    case 'W':
                    case 'X':
                    case 'Y':
                        num = (num*10)+9;
                        break; 
                    default:
                        num = num;
                        break;
                }
            }
            
            if(num == serial){
                out << names[i] << endl;
                find++;
            }
            
        }
    }
    
    if(!find)
        out <<"NONE" <<endl;
    
    return 0;
 }
    

